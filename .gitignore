# dependencies / build
node_modules/
/dist/
/coverage/
*.tsbuildinfo

# research scratch — clones, master.mdb, personal exports (NEVER commit; contains friend viewer IDs)
spikes/

# safety net: raw UmaExtractor exports dropped at root
/data.json
# UmaExtractor sample export (personal account data — sample for M1 import work)
docs/UmaExtractorSampleData/
*.local

# editor/os
.DS_Store
Thumbs.db

# Brainstorm visual companion (local mockups)
.superpowers/

# generated spec PDFs (regenerable: pnpm specs:html + browser print-to-pdf)
docs/superpowers/specs/*.pdf

# tesseract OCR training data (large fetched blob, not project source)
eng.traineddata

# Claude Code local state (git worktrees live under .claude/worktrees/)
.claude/

# Local Windows dev-server launcher (reads gitignored .env.local)
dev.bat

# Design-handoff preview vendor (1.1MB React; preview-only, not needed in repo)
docs/modules/design_handoff_support_card_builder/_ds/**/react.js

# design-sync (claude.ai/design) — staged scripts, build output, machine state.
# Durable inputs under .design-sync/ (config.json, NOTES.md, entry.ts, previews/)
# are committed; only the machine/cache dirs below are ignored.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
