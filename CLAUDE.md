# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Current State

**Status (2026-06-21):** all four modules in progress on `main` (**723 tests**, typecheck + build green; the recent + prior M4 feature tracks were code-reviewed and a review-fix + dedup cleanup pass landed — engine `simulatableBase` coverage, `CmRef` import hardening, `useRaceCompare` mood key, shared `nullsLast`/`useStreamingRank`/`useDismissOnOutside` helpers). **M4 is rebuilt around umalator's _vendored_ UI.** `/` is now `CmPlannerPage`: a saved-plan inventory rail with JSON/ZIP transfer, a full-height light CmPlan sidebar with stat growth + pink-spark target summary, the real umalator §0 race-track visualizer, a **timeline-driven** race-setup card, engine-ranked **unique + acquirable** skill charts, skill **effect-chips** in the detail viewer, and **skill-detail graphs** inside each `SkillDetailDisclosure` — a "Simulated performance" section with a **summary block** (avg バ身 gain, biggest gain + fire position, integer ×1/×2 fire-count breakdown), a **windowed velocity-vs-time** chart (zoomed to the activation, floored y, with-line trimmed at re-convergence; Worst/Typical/Best picker), the umalator-style **length-by-activation-position** + **activation-frequency-by-position** columns from N samples, and a **"?" help popup**. The old engine-driven skill chart (`/legacy` · `src/features/skill-acq/`) was **removed 2026-06-20**. Rebuild spec: [2026-06-16-m4-umalator-build-foundation-design.md](docs/superpowers/specs/2026-06-16-m4-umalator-build-foundation-design.md). **CM race ← timeline SSOT (2026-06-20):** the race chooser flows from the M3 timeline (not a hardcoded preset list); `CmPlan.cmRef` is now a `CmRefV2` union (`cm` stores geometry + **derives** conditions from the timeline; `custom` stores both), single-state derived view, `currentCm`-driven defaults, Dexie **v4**. Spec: [2026-06-20-cm-race-timeline-reference-design.md](docs/superpowers/specs/2026-06-20-cm-race-timeline-reference-design.md).

**Engine cooldown-aware multi-fire (2026-06-22):** the vendored engine now models cooldown-based skill re-activation — short-cd `all_corner_random`/`straight_random` skills (Professor of Curvature, Corner/Straightaway Adept, Beeline, Rushing Gale, …) fire the correct **per-course** number of times (Prof 2× on Hanshin 3200m, 1× on a mile). Flag-gated `cooldownReactivation` on `SimulationSettings`, **default ON** (engine-wide — skill-compare/vacuum/planner), OFF reproduces upstream single-fire exactly (the fidelity backstop). Distance-scaled cooldown `(cooldownTime/10000)·(distance/1000)` s, gated on real race time. The §0 overlay surfaces every fire; the skill-detail ×1/×2 breakdown shows the real double rate — it's a *distribution* (~21% double for Prof on Hanshin 3200m: **best-run shows 2 markers, typical shows 1**, which is honest, not a bug). **Engine source change is gitignored** (vendored clone) → captured in `engine-patches/2026-06-22-multifire.patch`; apply it then `pnpm sim:build`. **733 tests.** Spec/plan: [design](docs/superpowers/specs/2026-06-22-m4-engine-multifire-cooldown-activation-design.md) · [plan](docs/superpowers/plans/2026-06-22-m4-engine-multifire-cooldown-activation.md). Continuous near-lane skills (Slipstream etc.) deferred — vacuum sim has no opponents; the gate is built to accept them later (spec §12).

**Main-page redesign (2026-06-24) — dual-build flip-card planner:** `/` (`CmPlannerPage`) went 4-column → **3-column shell** (`inventory · plan-sidebar · main`). The sidebar is a **flip card** (`PlannerSidebar`: UMA1 blue / UMA2 red toggle, `--uma-accent`) over a **dual active-plan model** in `ActivePlanContext` (`uma1Plan`/`uma2Plan`/`focused`; `plan` kept as a deprecated alias of `uma1Plan`). **uma2 is session-scratch** — starts `null` each load, never persists its slot id / never writes `activePlanId`, but autosaves+autonames to inventory while active. Copy buttons: Duplicate uma1→uma2 (flips focus) / Replicate uma2→uma1 (confirms; preserves uma1 id+planNumber; via `copyPlanInto`). The **whole working context follows the flip** (sidebar + charts + checkers + track + auto-apply-track rule), all null-safe `focusedPlan ?? plan`. Inventory is **collapsible** to a sliver (`cmPlannerInventoryCollapsed`). The main column **pins track+race-setup** and tabs the rest: **`WorkingTabs` = Unique · Stamina · Accel · Skills · Mini-sim** (lazy single-mount). Two **engine-derived checkers**: **Stamina** (`staminaVerdict` over the HP trace from `runSkillTrace(planToOverlayBuild(plan)).without`, anchor chosen absent from the build so it traces the *wishlist* build incl. recovery) and **Accel** (`classifyAccelTiming` over `skillImpact` positions vs the final-straight `fs` via `findLast`). **`RaceSimCard`/`Uma2PickerPopover`/`Uma2Card`/`resolveUma2` are DELETED** — the two-build compare is now the **Mini-sim tab** (`MiniSimTab`), uma2 = the uma2 slot, overlay still on the §0 track. Seeded `:root` M4 design tokens (`--cmp-*`) in `cm-planner.css` (Phase-1 design-system candidates). **783 tests.** Spec/plan: [design](docs/superpowers/specs/2026-06-24-m4-main-page-redesign-design.md) · [plan](docs/superpowers/plans/2026-06-24-m4-main-page-redesign.md). **Next spec:** the dedicated **full race-sim page** (richer umalator-style sampling) — not built; Mini-sim is the lightweight compare. Deferred (final-review-accepted minors): empty `.cmp-copy-row` for prop-less callers; `WorkingTabs` empty-array guard; accel shows ALL wishlist+unique skills (not just speed/accel) + first-fire only.

**Inventory + sidebar UX refinements (2026-06-25):** a polish pass on the dual-build planner ([spec](docs/superpowers/specs/2026-06-24-m4-inventory-sidebar-refinements-design.md) · [plan](docs/superpowers/plans/2026-06-24-m4-inventory-sidebar-refinements.md)). **Inventory:** outline **backpack icon** (header title is text-only; the icon heads the collapse button + the **square** collapsed sliver — `--cmp-sliver-width` var, 1.5× icon); an **edit-mode pencil** gates the per-group/per-item download+delete buttons (document-`pointerdown` + `[data-edit-stay]` closest-check dismiss); hover-only **"1"/"2" slot-pick badges** (sized to the delete button so they share its footprint, accent border, hidden in edit mode) load uma1/uma2, the **row body loads the focused slot**, and loaded rows **glow** blue/red (`is-uma1`/`is-uma2`; old `is-active` removed). **Context:** `loadPlanIntoSlot(id, slot)` with **collision auto-duplicate** (`shouldDuplicateForSlot`+`copyPlanInto` → slots never share an id); `deleteSavedPlan` no longer mutates loaded slots; `isSaved` is **focus-aware**; **uma2 Save/Save As/auto-save now MIRROR uma1** — `saveUma2Plan`/`saveUma2PlanAs` persist to inventory but never write `activePlanId`, and uma2 autosave + pagehide flush are now gated on the shared `autoSave` flag (the old "uma2 autosaves unconditionally" is gone). **Sidebar:** `StatInput` (borderless, clear-to-empty, strips leading zeros, blur→0; component **file `StatInputField.tsx`** — Windows case-FS, see gotcha); Replicate folded into the action row with Save/Save As **enabled** for uma2; blank-face "⇋ Duplicate Uma 1"; `trackMismatchLabel` chip; full **red `--uma-accent`** across name/note/search/wishlist-search inputs, apt/mood/strategy tiles, auto toggles, the selected Role button, and the flip-card header border (stat fields kept clean). **Page:** the **track-change confirm** (`autoApplyOn` unified read, `trackOverrideRef` pin, `applyTrackTransition` for flip+load) is a **centered dialog over a greyed-out track** naming the slot ("Change track to Uma 1/2's race?") + the 3s "Track changed!" transient. Pure helpers `statInput`/`trackChange`/`slotLoad`. **824 tests.** (Save-As leaves the copy auto-name OFF by design — the dedup `(N)` suffix ≠ the generated name.)

**WorkingTabs stamina/accel rebuild (2026-06-25):** the M4 working tabs gained engine-backed **Stamina + Accel** tools, replacing the old timing-classifier checkers ([spec](docs/superpowers/specs/2026-06-24-m4-workingtabs-stamina-accel-design.md) · [plan](docs/superpowers/plans/2026-06-24-m4-workingtabs-stamina-accel.md)). **Tabstrip:** keep-alive (visited tabs persist across switches), per-tab orange ⚠ stale marker, equal-width tabs, invisible lower border, and a `HeaderHelp` "?" popup per chart (**portal + fixed-position** so it escapes the card's `overflow:hidden`). **Uma chart** runs in the background + persists across tab switches, clearing only on track change. **Stamina Calculator (`StaminaSpurtTab`):** Monte-Carlo full-spurt-rate + stamina-survival readout with **two independent persisted targets** — full-spurt-rate (`useStaminaSpurtTarget`) and stamina-survival (`useStaminaWarnThreshold`), **separate localStorage keys**; each shows "stamina needed for X%" + the finish-HP min/median/mean/max **sampled at that required stamina** (`requiredStaminaForSpurt` now takes a `{rate, finalHp}` probe and works for either metric). Plus a downhill-saving range, opponent stamina-debuff injection (`buildInjectedDebuffs`, white `201222` / gold `201221`), and a finish-HP histogram. **Accel (`AccelChartPanel`):** an accel-only filtered skill chart (effect = accel×duration, positioning → CM-9 / LoH-12 places, wit-check), replacing the classifier; `StaminaCheckerTab`/`AccelCheckerTab`/`accelCheck`/`staminaCheck` are **DELETED**. **Skill + Accel** stamina-out banners bind to the **survival** target. Engine: per-sample finish HP + downhill / staminaDrain / injectedDebuffs surfaced through `runVacuumCompare(opts: VacuumOpts)` (engine-patch `engine-patches/2026-06-24-stamina-finalhp.patch`; `pnpm sim:build`). **850 tests**, typecheck + build green; merged to `main` 2026-06-25.

**Design system (2026-06-25):** Phase 1 design-system foundation landed. Themeable (light default + dark) semantic tokens in `src/styles/design-system/`, `ds-*` component classes, a `/styleguide` route, and a Light/Dark/System toggle in settings. Existing token names kept (271 usages untouched); M4 + app shell verified in both themes with no light regression. **862 tests.** Spec/plan: [design](docs/superpowers/specs/2026-06-25-design-system-foundation-design.md) · [plan](docs/superpowers/plans/2026-06-25-design-system-foundation.md).

**M1 Inheritance workbench — build started (2026-06-26):** the M1 (Inheritance) module is now the active build, card-by-card (M1.0–M1.8) from the 2026-06-25 design handoff at [docs/modules/design_handoff_support_card_builder/](docs/modules/design_handoff_support_card_builder/). **M1.0 (core math, logic-landed 2026-06-25):** `src/core/winBonus.ts` (affinity **2.0** — only shared **G1** races count, **+3 each**; G2/G3 + Triple-Crown/Tiara titles → 0; patch applies Global 2026-07-01 11:00 UTC), `lineageAffinity.ts` planner adapter, `spark.ts` per-member affinity resolver (de-approximates when a computed score is supplied), `Parent.wonRaces`/`ParentRef.wonRaces`, and a rental target-tier helper (`affinityNeededForTier`). Live pending the UmaExtractor `wonRaces` import + the S1 base-relation data refresh. **M1.1 (shell + route, landed 2026-06-26):** un-stubbed `/inheritance`; `src/features/inheritance/` with the 3-col workbench grid (collapses <1120px) + the **plan-context header** (`PLAN #N` · name · "From CM Planner · {track} Racecourse" · surface/distance/strategy chips) reading `uma1Plan`; track resolved via lazy `courseCatalog` (injectable `deps`), distance via core `distanceClass` (game-correct). Remaining column panels are labeled placeholders that M1.3–M1.8 replace. **M1.2 ("Uma plan" card, landed 2026-06-26):** a `cmp-plan-card` (header "Uma plan") in the left column — portrait + name/epithet + the plan's three active aptitude chips. **Chips = `umaPlanAptChips(plan)` = distance/surface/strategy via `currentAptitudeKeys`+`targetAptitude`** (stored pink goal else default S/A/A), so all three always render and follow the selected plan — NOT `sparkGoals.pink.map` (that showed only explicitly-stored goals → "only the strategy chip"), and NOT the uma's `baseAptitudes`. A bigger **inventory-icon button** in the header pops the **shared `PlanInventoryCard`** (`@/features/cm-planner/PlanInventoryCard`) as a **dismiss-on-outside popover anchored to the icon** (`useDismissOnOutside`, esc-closable; the popover sits in an `inh-uma-inv-anchor` span so its top-right corner is at the icon's bottom border — the card is `overflow:visible` with re-rounded header corners so it isn't clipped). Clicking a row loads that plan as current (uma1) and closes the popover; the inventory's **1/2 slot badges** + **"Inventory settings" sub-card** are hidden via new `hideSlotBadges`/`hideSettings` props (default off → M4 unchanged). The page wires the inventory to the shared active-plan context (uma1 slot: `uma1Plan`/`uma2Plan`/`savedPlans`/`loadPlanIntoSlot`/`deleteSavedPlan`/`importSavedPlans`/`deleteAllSavedPlans`, `focused="uma1"`). `UmaPlanCard` is presentational/provider-free (portrait + the wired inventory node passed in as `ReactNode`s; open state controlled so the page closes it after a pick). **(UI iterated on feedback: bespoke `SearchPicker` card → full inventory-card-in-column → inventory-icon popover.)** **M1.3 ("Plan targets" card, landed 2026-06-26):** collapsible `cmp-plan-card` in the left column — **blue** stat-spark goals (`sparkGoals.blue`) with `−/N★/+` steppers (clamped 0–9) + `×` delete + add-stat select; **pink** aptitude sparks display-only (reuses `umaPlanAptChips`); **wishlist** rows (skill name + SP, gold-tinted) under a "{n} skills · {SP} SP" headline. Pure helpers `planTargets.ts` (`blueSparkRows`/`setBlueStars`/`addBlueSpark`/`deleteBlueSpark`/`wishlistRows`/`wishlistSummary`); presentational `PlanTargetsCard` wired to `setPlan` + gameData `skillById`. **882 tests.** Plans: [m1-1-workbench-shell](docs/superpowers/plans/2026-06-26-m1-1-workbench-shell.md) · [m1-2-uma-plan-card](docs/superpowers/plans/2026-06-26-m1-2-uma-plan-card.md) (early version; UI iterated past it). **Next: M1.3 "Plan targets" card.** ⚠️ M1 conventions: (1) component files dodge the Windows case-FS clash with pure-helper siblings — `PlanContextHeaderView.tsx`/`planContextHeader.ts`, `UmaPlanCard.tsx`/`umaPlanApt.ts`; (2) presentational cards are **provider-free** where practical, else the page guards/mocks (`GameIcon`/`useGameData` throws without a provider). Shared `STRATEGY_LABEL`/`cap` in `src/features/inheritance/labels.ts`. **M1 reuses M4 surfaces directly** (the inventory card) rather than rebuilding — prefer this.

**Per-module detail lives in [docs/modules/](docs/modules/) — load the lean file below + the one module you're working on, not the whole history.**

| Module | Route | Status | Doc |
|---|---|---|---|
| M4 Skill Acquisition | `/` (rebuild) | **Planner rebuild + inventory + skill-detail graphs + timeline-driven race, current through 2026-06-20:** saved-plan inventory/JSON transfer, full-height CmPlan sidebar, base-aptitude pink-spark summary, vendored track, **timeline-driven race setup (`CmRefV2` union, single-state, `currentCm` defaults)**, unique-skill chart, acquirable-skill L/SP/efficiency chart (**gold rows price the bundled white prereq via `purchaseSpCost`**), and **velocity + length/frequency-by-activation-position graphs in skill disclosures** at `/`. The old `/legacy` engine chart + sourcing (`src/features/skill-acq/`) were removed 2026-06-20. **Redesigned 2026-06-24:** 3-column dual-build flip-card shell (uma1 blue / uma2 red), collapsible inventory, `WorkingTabs` (Unique · Stamina · Accel · Skills · Mini-sim), engine-derived stamina + accel checkers; `RaceSimCard` retired → Mini-sim tab. **UX refinements 2026-06-25:** slot-pick 1/2 badges + collision auto-duplicate, edit-mode gate, backpack icon, uma2 save mirrors uma1, centered track-confirm dialog, full red uma2 accent. | [module-4](docs/modules/module-4-skill-acquisition.md) *(detailed)* |
| M1 Inheritance | `/inheritance` (shell) | **Active card-by-card build (handoff). M1.0 core math + M1.1 shell/route/header + M1.2 uma-plan card + M1.3 plan-targets card done (882 tests); M1.4 inheritance/parents card next.** | [module-1](docs/modules/module-1-inheritance.md) |
| M2 SP Optimizer | `/sp-optimizer` | MVP shipped; F1–F4 follow-ups | [module-2](docs/modules/module-2-sp-optimizer.md) |
| M3 Meta Intel | `/meta-intel` | Timeline + synthesis shipped; phase-2 next | [module-3](docs/modules/module-3-meta-intel.md) |

**⚠️ Design fidelity — the mockups are the spec.** The built module UIs are **functional skeletons far below the design mockups** that capture the product vision: **[docs/mockups/](docs/mockups/)** (committed; the canonical visual spec — open the `.html` in a browser). Fidelity audit 2026-06-15: **M4 ~25%, M3 ~25%, M2 ~20%, M1 ~8%.** The gap was classified — and **~80% of it needs no new data:**
- **Design system** — one shared dark token + badge/chip/effect/track visual grammar, *identical across all 4 mockups*; building it once re-skins everything. (Currently mostly absent.)
- **Not-built but data exists** — M4 §0 track diagram (**DONE 2026-06-16 — vendored umalator track**), M4 left-panel planner cards (**DONE 2026-06-16 — CmPlan sidebar**), M4 sourcing table, **M2 results table + Compare-vs-Veteran** (`runPlannerCompare` exists), M3 grid-with-month-columns layout, M1 pedigree + goal-builder + compare-all. Buildable now.
- **Genuinely data-gated:** (1) skill effect-type/variants → **effect-chips DONE 2026-06-16** (effect/variant data baked into `skills.json`); velocity/L-vs-distance/activation-rate graphs → **DONE 2026-06-17** (`runSkillTrace`/`skillImpact`, `useSkillTrace` hook, SVG charts — no new data; VFalator is a umalator fork so the trace was already in the bundle); duration labels still open; (2) uma base aptitudes/stat growth → **DONE 2026-06-17** (`build-umas` bakes GameTora `aptitude` / `stat_bonus` into `umas.json`); innate skills + usable-here filters still open (M4/M1); (3) per-record release dates → Now/Upcoming/Future (M4); (4) banner + patch timeline entries → M3's empty lanes.

**Treat the mockup HTML as the spec; the design system is a first-class deliverable, not deferred** (the earlier "Slice 1 = functional minimum" scoping under-delivered on the visual vision). Recommended realignment: shared `design-system.css` → M4 to full mockup fidelity → the remaining high-value data tasks (skill duration/graphs, innate/release data) → M2/M3/M1.

**Shared foundation:** vendored umalator engine v0.14.2 (`src/sim/`, `pnpm sim:build`; `evalSkillDelta` / `runVacuumCompare` / `runPlannerCompare` + `SimClient` worker + L-cache; fidelity-verified vs upstream, meanBashin 0.2202 on the `cooldownReactivation:false` path; the engine carries a local **cooldown-multi-fire** patch as of 2026-06-22 — `engine-patches/`, flag default-ON), the canonical `CmPlan` SSOT ([`docs/superpowers/specs/2026-06-15-shared-data-model.md`](docs/superpowers/specs/2026-06-15-shared-data-model.md); Dexie **v4** — `cmRef` is the `CmRefV2` union as of 2026-06-20), and pure mechanics in `src/core/`. All four module designs are locked (`docs/superpowers/specs/2026-06-14-m{1,2,3,4}-*.md`).

**Next (design-fidelity realignment — chosen direction 2026-06-15):** the built UIs are far below the mockups (see *Design fidelity* above). Recommended track: shared `design-system.css` (re-skins all 4) → M4 to full mockup fidelity → remaining data tasks (skill duration/graphs, innate/release data) → M2/M3/M1. The module-feature backlog still stands underneath: M1 Plans 3–5 ([module-1](docs/modules/module-1-inheritance.md)), M4 Slice 1b, M2 F1–F4, M3 phase-2.

**M4 rebuild — next up (2026-06-20):** the primary planner surface, saved-plan inventory/JSON transfer, the engine-ranked **unique-skill chart** (`UmaChartPanel`/`rankUmaChart`/`useUmaChart`), the acquirable **white/gold skill chart** (`SkillChartPanel`/`rankSkillChart`), **skill effect-chips**, the **timeline-driven race setup** (`CmRefV2` union + single-state derived view + `currentCm` defaults — see the M4 doc's *Race setup ← timeline SSOT*), and the **skill-detail graphs** are live + **polished (2026-06-20)**. In each expanded `SkillDetailDisclosure`, a **"Simulated performance"** section: a **summary block** (avg バ身 gain = `meanL` 2dp · biggest gain + exact fire position = `peakImpactPosition` · integer **×1/×2 fire-count breakdown** = `activationCounts`), a **windowed velocity-vs-time** chart (utools/kachi-style — `velocityWindow` zooms to the activation ±10s, floors the y-axis, trims the with-skill line at re-convergence; Worst/Typical/Best picker in the chart title), the umalator-style **length-by-activation-position** + **activation-frequency-by-position** columns (`skillImpact` → N-sample `{horseLength, positions}` binned by fire-position), and a **"?" help popup** (click-outside closes). `useSkillTrace` hook, `TRACE_SAMPLES=20`/`IMPACT_SAMPLES=400`, **LRU-memoized**, hand-rolled SVG; wired in `UmaChartPanel` accordion + `PlannerSidebar` (both honor main's `collapseSkillSignal`). **⚠️ Multi-fire:** the **engine now models cooldown re-fire** (2026-06-22, flag `cooldownReactivation` default ON) so the position/frequency charts + the §0 overlay show real multi-fire; the skill-detail **velocity-window chart + `peakImpactPosition` still use the first activation (`acts[0]`)** — that UI niceness is the remaining limitation. **Two-build race-sim compare + on-track overlay SHIPPED 2026-06-21** ([spec](docs/superpowers/specs/2026-06-20-m4-two-build-race-compare-track-overlay-design.md), [plan](docs/superpowers/plans/2026-06-20-m4-two-build-race-compare-track-overlay.md)) — this subsumes the former *full-race uma1-vs-uma2 compare* and *HP/velocity/skill-activation racetrack overlay* resume items: the §0 track **is** the umalator main view now. Pick uma2 (a saved plan) in the right-sidebar **Race sim** card (`RaceSimCard`, an inventory-style popover picker + red uma2 card) and `RaceOverlay` draws, directly **on** the `RaceTrackView` SVG, both builds' velocity + HP curves, a speed (m/s) y-axis, **umalator rung-stacked** skill markers (greedy interval-coloring; red uma2 stacked above blue uma1), and the バ身-gap sub-band. Engine: `planToOverlayBuild` (uma + unique + wishlist active) → `runRaceCompare` (both runners' per-frame trace + all-skill activations + per-frame gap) via worker/`SimClient.raceCompare`; `useRaceCompareController` lifts the shared state (overlay on the track ← controls in the right sidebar). Resume with: **velocity-chart multi-fire** (widen window to span all procs — user undecided), **card-hint sourcing (§3)**, the uma **innate/release/usable-here columns**. Handoffs: [module-4](docs/modules/module-4-skill-acquisition.md), [skill-detail graphs](docs/superpowers/plans/2026-06-20-m4-skill-detail-graphs-handoff.md), [inventory](docs/superpowers/plans/2026-06-18-m4-plan-inventory-card-handoff.md), [sidebar](docs/superpowers/plans/2026-06-18-m4-sidebar-polish-handoff.md), [track setup](docs/superpowers/plans/2026-06-18-m4-track-setup-card-handoff.md). **Also merged 2026-06-19 (skill/unique chart polish):** Run→Stop button, persistent run-status + orange "Changed detected" stale prompt, italic captions, skill-chart 5 rarity tabs (all/non-unique/inherited/white/gold via family×rarity reps), proc-aware "show not-activatable" (`BashinStats.activated` from the engine's `skillActivations` → `inactive` rows hidden+dimmed), sort-invert, and a uma "Rank by style" dropdown. **Force-activation (VFalator "best-case")** was investigated and deferred — feasible but the exported `runSkillComparison` ignores forced params, so it needs a `sim:build` rebuild. Handoff: [skill-chart polish + force-activation](docs/superpowers/plans/2026-06-19-m4-skill-chart-polish-and-force-activation-handoff.md).

**Cross-cutting gotchas** (module-specific ones live in each module doc):
- **GitHub Pages must use the "GitHub Actions" source (`build_type: workflow`), NOT "Deploy from a branch" (legacy Jekyll).** On legacy, every push *also* triggers GitHub's default Jekyll `pages-build-deployment`, which (a) fails because Jekyll parses our markdown docs as Liquid and chokes on `{{ … }}` (e.g. `docs/superpowers/plans/2026-06-15-m3-timeline-ui.md`), and (b) would publish raw docs instead of the Vite app even if it succeeded. Fix once: `gh api --method PUT repos/<owner>/<repo>/pages -f build_type=workflow` (done 2026-06-16). Our `ci.yml` `deploy` job is the only intended publisher.
- `git worktree` dirs under `.claude/worktrees/` may fail to auto-remove on Windows (node_modules lock) — git de-registers them but delete the folder by hand.
- **Vitest flakes while a `pnpm dev` server is running** — a transform/HMR race makes UI test files fail en masse on the first run with `Cannot read properties of null (reading 'useState')` (React resolves to null), then pass on a clean re-run. `pnpm build` (typecheck + vite) and `pnpm typecheck` are race-free — trust those, and re-run the failing test file before treating a failure as real.
- **Planner-page card grammar (`cm-planner.css`):** the right-column panels (track / race-setup / unique-skill chart) and the sidebar all share `cmp-plan-card` (1px border + 12px radius + soft shadow + `bg-1`) wrapping a `cmp-plan-card-head` gray bar + `cmp-plan-card-body`. Collapsible panels make the head `cmp-collapse-head` (+ a `cmp-collapse-caret` that flips on `data-open`) so the **whole header toggles**; interactive controls inside it (e.g. the chart's Run button) must `stopPropagation`. Reuse this for any new planner panel — don't reintroduce the old `panel`/`cmp-uma-head` one-offs.
- **Planner page is a 3-column grid (`cm-planner.css`, redesigned 2026-06-24):** `inventory · plan-sidebar · main` — the old 4th `.cmp-right` race-sim rail + `RaceSimCard` are **gone**. Collapsing the inventory sets `data-inv-collapsed` on `.cmp-page` (col 1 → sliver). The main column **pins the track + race-setup** and tabs the rest via **`WorkingTabs`** (Unique · Stamina · Accel · Skills · Mini-sim, lazy single-mount); the compare controls are the **Mini-sim tab** (`MiniSimTab`) and **uma2 = the `uma2Plan` slot** (no picker; `useRaceCompareController(uma1Plan, uma2Plan, courseId)`). The race-compare **overlay is drawn ON the §0 track**, not in a separate panel: `RaceTrackView` takes optional `trace`/`traceDistance`/`showHp`/`skillName` props and renders `RaceOverlay` on top **inside the same viewBox** — `ViewHeight` stays 240, the no-trace track is byte-identical. Shared state is lifted to `useRaceCompareController` (overlay reads the trace in `.cmp-main`; controls live in the Mini-sim tab). Skill markers use **umalator rung-stacking** (greedy interval-coloring by bar-OR-label extent → no overlaps; `RaceOverlay.placeRungs`). **Gotcha:** `runRaceCompare` is the vacuum-compare entry (`runComparison(uma1, uma2)`) — both runners are independent vacuums, so it populates `skillActivations` for both; the gimped `runSkillComparison` does NOT. **LRU sigs:** `useSkillTrace` + `AccelCheckerTab.impactSig` now key on the build's **`skills` (sorted)** — this is the 2026-06-24 fix for the stamina/accel checkers returning a **stale verdict when the wishlist changes** (the sig previously omitted skills); `useRaceCompare` keys aptitudes+mood; `useSkillTrace` still omits aptitudes/mood (pre-existing, lower-impact).
- **uma2 save now mirrors uma1 (2026-06-25).** uma2 is still session-scratch (cleared on reload, never writes `activePlanId`), but its **Save/Save As are enabled** (`saveUma2Plan`/`saveUma2PlanAs` — persist + refresh + update the slot, no `activePlanId`) and its **autosave + pagehide flush are gated on the shared `autoSave` flag** like uma1 — NOT unconditional anymore. Don't reintroduce the old always-autosave `setUma2Plan` path; root-caused from the toggle looking inert on the uma2 face while it silently drove uma1.
- **The stat-input component is `StatInputField.tsx` (exports `StatInput`), NOT `StatInput.tsx`.** On Windows' case-insensitive FS, `import './StatInput'` resolves to the `statInput.ts` helpers (no `StatInput` export → the component renders `undefined`). Keep the `Field` suffix.
- **Collapsed-sliver CSS needs the compound `.cmp-plan-inventory.cmp-inventory-sliver` selector** — the plain `.cmp-plan-inventory { padding-right: 0.15rem }` rule lives later in `cm-planner.css` at equal specificity and would otherwise win and re-add the gutter (bit the collapsed inventory↔sidebar gap twice). The collapsed grid column **and** the sliver width both read `--cmp-sliver-width`.
- **Inventory edit mode** (`PlanInventoryCard`) gates the destructive buttons behind a pencil toggle; it exits on a document `pointerdown` whose target is **not** inside a `[data-edit-stay]` element — tag any new edit-only control (and the pencil itself) with `data-edit-stay`. The hover-only `.cmp-slot-badge` "1"/"2" buttons are sized to `.cmp-inventory-icon-btn` (1.85rem/7px) + same container gap so they occupy the exact footprint of the edit-mode download/delete buttons.
- **`CmPlan.cmRef` is a `CmRefV2` union, and the timeline owns CM conditions (2026-06-20).** A `kind:'cm'` ref stores geometry (`courseId`/`surface`/`distance`) but **never** conditions — ground/weather/season are **derived** from the matching timeline entry at read time. A `kind:'custom'` ref stores both. So: (1) **don't store conditions on a `cm` ref** or you reintroduce the drift this replaced; (2) the M4 race panel is a **derived view of `plan.cmRef`** (no parallel selection state — `cmRefToSelection`/`selectionToCmRef` in `src/features/planner/race-setup/cmRefSelection.ts`, pure logic in `src/core/cmRace.ts`); (3) reading `surface`/`distance`/`courseId` needs no narrowing, but reading conditions does (cm → derive, custom → stored); (4) any new `cmRef` source (import, fixtures) must pass through `normalizeCmRef`. Adding a CM to the M3 timeline now auto-populates the race chooser — no `PRESETS` edit. Dexie schema is **v4**.
- **`src/app/App.tsx` is the merge-collision point** when two module branches each add a nav route — resolve by keeping *both* NavLinks/Routes and trimming `STUB_MODULES` (only `Inheritance` remains a stub now).
- **The sim engine throws `firstPositionInLateRace` on a 0-speed runner** — never feed `evalSkillDelta` an all-zero build; `makeDefaultPlan` seeds non-zero stats and the M4 chart guards `spd > 0`. The engine **also throws on an engine-unknown skill id** in a runner's deck — every comparison entrypoint (`evalSkillDelta`/`runVacuumCompare`/`runRaceCompare`/`runSkillTrace`/`skillImpact`) wraps its build in `simulatableBase` to drop those ids, so an inherited-unique/JP-only/imported id can't blank a result (the latter three were the gap closed in the 2026-06-21 review-fix pass).
- The pre-engine M4 coverage UI (old `SkillPlannerPage` + `src/features/coverage/` + `src/features/inventory/` + the `iconRetrofit` integration test that only exercised them) was **removed 2026-06-15** once the engine chart replaced it — recover the coverage-matrix/inventory pattern from git history if Slice 1b needs it. `PlanHeaderPanel`/`SkillPicker` stayed (shared by the new page).
- **Don't put the global `.page` class on a full-width *stacked* page** — at wider widths `.page` becomes a **2-column grid** (legacy coverage layout, `src/styles/app.css`), which lays children out side-by-side. The M4 rebuild page uses its own `.cmp-page` (flex column). (This bit us: the track + race-setup rendered side-by-side.)
- **The vendored bundle's `coursesService` exposes `getAll()` / `getAllEntries()` / `getByTrackId()` at runtime** even though the `.d.mts` originally only typed `getSimCourse` — widen the `.d.mts` and call them (no `pnpm sim:build` rebuild needed). `src/sim/courseCatalog.ts` enumerates all 107 courses / 11 tracks (incl. Ooi) from these for the custom-track picker.
- **The exported `runSkillComparison` (skill A/B L) ignores forced-activation params** (`forcedPositions`, `scenarioOverrides.forcedRank`) — unlike `coursesService.getAll()`, these are NOT honored at runtime (the source wires them only to `runComparison`/`runSampling`, not the skill-compare entry; proven by spike — passing them changed nothing). The skill compare also runs **solo** (your runner vs the same runner+skill, no opponent field), so position-relative conditions like `order_rate≤40` never trigger → such skills come back `activated:false` and are classified `inactive` (hidden by default). The `activated` flag itself IS available — it's derived from the engine's `skillActivations` in `bashinStatsFrom` (`src/sim/adapter.ts`). **VFalator-style "force at meter" needs a `pnpm sim:build` rebuild** to expose a forced skill-L entry (or the low-level `RaceSolverBuilder` + a fixed-`Region` `ActivationSamplePolicy`). Readable source in `spikes/repos/{umalator-global,uma-skill-tools}/`.
- **Keep the engine lazy** — reach engine-backed data via lazy `import('@/sim/courseData')` / `import('@/sim/courseCatalog')`, **not** the `@/sim` barrel (the barrel's `run`/`adapter` pull the ~5 MB engine bundle synchronously into the importing chunk). Verified: those stay tiny separate chunks.
- **Vendored umalator racetrack layers carry `// @ts-nocheck`** (3 files under `src/features/planner/racetrack/vendor/layers/`) — verbatim third-party SVG that trips our strict `noUncheckedIndexedAccess`; the pragma is scoped to those files only.
- **Hand-rolled M4 §0 track retired (2026-06-16)** — `src/core/track.ts`, `src/sim/courseGeometry.ts`, and `TrackDiagramPanel` were deleted and replaced by the vendored umalator track (`src/features/planner/racetrack/`).
- **`runComparison` returns no aggregate stats** — `min/max/mean/median` are NOT fields on the engine result object; derive them from the sorted `results` array. Also: full-comparison `runData` carries per-frame traces for both runners; skill-comparison `runData` carries only `sk` activation logs — do not conflate when extracting skill-trace data (used by `runSkillTrace` / `skillImpact` in `src/sim/run.ts`).
- **Open `SkillDetailDisclosure` with `traceContext` crashes jsdom** — constructing `SimClient` in jsdom environment spawns a real Worker (jsdom doesn't support it). Any test that opens such a disclosure must `vi.mock('./useSkillTrace')` to stub the hook (see `UmaChartPanel.test.tsx`, `PlannerSidebar.test.tsx`).
- **Skill-detail velocity chart is intentionally velocity-vs-_time_** (VFalator/utools parity — reference: `spikes/repos/kachi-uma-tools/umalator/app.tsx` `VelocityChart`), **not** vs distance. Velocity-vs-_distance_ + HP belongs on the §0 racetrack overlay, not here (prototyped + reverted; see the racetrack-overlay handoff).
- **The 4 representative runs (`min/median/max/mean`) are ranked by バ身.** For a skill that procs < 100% of the time, the **Worst and Typical** runs are usually **non-firing samples** (with == without _identically_, no divergence → the windowed velocity chart falls back to the whole race). Only **Best** is guaranteed to be a firing run; the section shows a "skill didn't fire in this run" note when the shown run has no activation.
- **Multi-fire skills (cooldown re-proc, e.g. Professor of Curvature on long tracks):** the **engine models the re-fire as of 2026-06-22** — flag `cooldownReactivation` on `SimulationSettings`, **default ON**; eligible short-cd `all_corner_random`/`straight_random` skills fire the correct per-course count (Prof 2× on Hanshin 3200m), OFF reproduces upstream. Total バ身 is whole-race-correct, and `impactByPosition`/`frequencyByPosition`/`activationCounts` + the §0 overlay reflect every fire. `velocityWindow` + `peakImpactPosition` still consider only the **first** activation (`acts[0]`) — the remaining UI limitation. **Engine logic lives in the gitignored vendored clone** (`spikes/repos/umalator-global/src/`), captured in `engine-patches/2026-06-22-multifire.patch` — edit the clone, re-apply/refresh the patch, `pnpm sim:build`, commit the regenerated bundle. Eligibility = policy exposes `sampleMulti` (corner/straight) **and** `cooldownTime < 5000000`; the gate is `(cooldownTime/10000)·(distance/1000)` s vs `accumulateTime.t`. The earlier "force-activation" framing is superseded for these skills (the engine genuinely fires twice now).
- **Skill-trace test files with ≥ 2 `render`-and-query tests need `afterEach(cleanup)`** — without it the DOM accumulates and `getByRole`/`screen` queries hit duplicate buttons across tests (bit us with the run-picker; `SkillTraceCharts.test.tsx` + `SkillTraceSection.test.tsx` now register cleanup).
- **The Stamina tab has TWO targets, bound to different things (2026-06-25).** `useStaminaWarnThreshold` is the **stamina-survival** target (its localStorage key `cmp.staminaWarnThreshold`) and is what the Skill/Accel **stamina-out banner** keys off — running out of HP is a *survival* failure, not a full-spurt shortfall. `useStaminaSpurtTarget` (key `cmp.staminaSpurtTarget`) is the separate **full-spurt-rate** target driving only the full-spurt required-stamina readout. Don't re-collapse them into one threshold; in `CmPlannerPage` the Skill/Accel `warnThresholdPct` = the *survival* %, not the spurt %.
- **Design system lives in `src/styles/design-system/` (tokens.css + components.css), imported in `main.tsx` BEFORE `app.css`.** Tokens are theme-scoped via `[data-theme]` on `<html>` (absent ⇒ light default; `theme.ts` + the settings `ThemeToggle` set it). The existing semantic token names (`--bg-0/1/2`, `--fg`, `--accent`, `--border`, `--warn*`, `--tier-*`) were **kept** and made theme-aware — don't rename them. Dark `--bg-2` is a *raised* grey (`#1d212a`) so all 271 legacy `--bg-2` usages read in dark without a per-usage audit; the mockup's deep-sunken `#0c0e13` is the new `--bg-3`. New component classes are `ds-*` prefixed. **Dark is guaranteed-correct only for the design-system primitives + app shell + M4 + `/styleguide`**; other modules' feature CSS still has hardcoded colours (their fidelity phase tokenizes them).
- **`StaminaSpurtTab` test gotcha: `getByText(/Spurt rate/i)` is ambiguous** — it also matches the "Stamina needed for X% full **spurt rate**" headlines, so a `waitFor(() => getByText(/Spurt rate/i))` finds multiple elements and never resolves (looks like a hang/timeout, not a component bug). Use the exact `getByText('Spurt rate')` for the stat-label, and assert target lines with their full text ("full spurt rate" / "stamina survival").

Key files:
- [docs/roadmap.md](docs/roadmap.md) — **the sequencing layer (design-fidelity-first): design-system → M4 deep → M2/M3/M1, + engine-update & public-data sidelists. Set 2026-06-24 after a hakuraku/UmaTools/TheCing competitive analysis (fork nothing; keep our engine; harvest hakuraku for M1).**
- [docs/engine-update-todo.md](docs/engine-update-todo.md) — **sidelist: bump vendored engine v0.14.2 → v0.18.0 (~7 releases behind upstream `jalbarrang/umalator-global`) + data refresh, gated behind a `fidelity.test.ts` re-baseline. NOT TheCing (sibling Preact fork, no versioned engine).**
- [docs/hakuraku-m1-harvest.md](docs/hakuraku-m1-harvest.md) — **what to port from hakuraku (MIT) into M1: new `src/core/sparkChance.ts` (individual-affinity spark proc), affinity race-bonus + same-chara-guard refinements, `rankParents` factor optimizer. Re-derive + validate vs umamily.moe, don't transcribe.**
- [docs/data-refresh-runbook.md](docs/data-refresh-runbook.md) — **how to refresh game data (pin bump), add a confirmed CM, or add an upcoming skill.**
- [docs/modules/](docs/modules/) — **per-module living docs (read first for any module).**
- [uma-cm-planner-plan.md](uma-cm-planner-plan.md) — the full project plan and single source of truth (Phase 0 checklist annotated with outcomes; §14 decisions annotated).
- [docs/provenance.md](docs/provenance.md) — engine vendor pin (umalator-global v0.14.2), licenses (clean GPL chain), all dataset sources/formats, UmaExtractor import spec, rental-site deep-link templates.
- [docs/mechanics-notes.md](docs/mechanics-notes.md) — verified mechanics numbers (cite these in core tests); §10 lists what still needs in-game verification.
- `spikes/` — scratch clones + research artifacts incl. a live Global master.mdb with extractions (`spikes/repos/umalator-global/db/`). **Gitignored.** Structured agent findings: `spikes/phase0-results.json` + `spikes/phase0-completion-results.json`.

Tooling note: git, Node 24, pnpm 10 (npm-global) present. Headless engine runs under plain Node need esbuild `define:{'import.meta.env':'{"DEV":false}','import.meta.main':'true'}` (see provenance §1.1). User exports with personal data live under `spikes/samples/` — gitignored, never commit.

The project: a local-first web app for Umamusume: Pretty Derby (Global) Champions Meeting build planning, with four modules — Skill Acquisition Planner (Module 4, build first), Inheritance Planner (Module 1), SP Purchase Optimizer (Module 2), and Meta Intel Workspace (Module 3).

## Commands

Stack: TypeScript + Vite + React 19, pnpm, Vitest (jsdom), Dexie. Path alias `@/*` → `src/*`.

```sh
pnpm dev          # dev server
pnpm test         # vitest run (single test: pnpm vitest run src/core/coverage.test.ts)
pnpm typecheck    # tsc --noEmit
pnpm build        # typecheck + vite build
pnpm data:build   # regenerate public/data/ from borrowed sources + data-overrides/ (tsx scripts/build-all.ts)
```

CI (`.github/workflows/ci.yml`): typecheck + test + build on every push/PR; GitHub Pages deploy from main (`BASE_PATH` env sets the Vite base).

**Remote access (Tailscale):** the dev server binds all interfaces on port 5177 and adds `TAILNET_HOST` (from the gitignored `.env.local`) to Vite's `allowedHosts`. **Whenever you start `pnpm dev`, also surface the tailnet URL `http://<TAILNET_HOST>:5177/`** (read the value from `.env.local`) so the user can open it from any device on their tailnet. It's tailnet-only, not public; true public exposure would be `tailscale funnel 5177` (don't enable without asking).

## Guiding Principles (standing rules — mirrored from plan §2, which is authoritative)

**P1. REUSE FIRST — always search before building.** The Umamusume community has 5+ years of tooling. Before implementing any mechanic, calculation, or dataset: (1) check the Resource Map in plan §3; (2) search the web for community tools beyond it; (3) prefer import as dependency > vendor/borrow data > port a known-good algorithm with attribution > build from scratch. When porting, record source URL + retrieval date in a code comment and in `docs/provenance.md`. **Keep the Resource Map alive:** any useful resource discovered in a session gets added to plan §3 with a "use for" note before the session ends.

**P2. Local-first, zero backend.** Static site; game data baked at build time; user data in IndexedDB; JSON export/import. No server-side anything.

**P3. Honest numbers.** Show real numbers where mechanics are calculable; show qualitative reliability tiers + evidence where they're RNG-dependent. Never fabricate precision. Simulations are estimations, not verdicts — surface caveats in the UI.

**P4. Server-versioned data.** Every skill/card/uma record carries `server: "global" | "jp"` and a `dataVersion`. JP-ahead content is preview-only, never silently mixed into Global calculations.

**P5. Hand-patchable data.** Every generated dataset has a sibling `*_overrides.json` merged last and maintained by hand. Never edit generated files (`public/data/`) directly.

**P6. Pure-function core.** All game-mechanics logic lives in `src/core/` as pure TypeScript functions with unit tests, validated against community references (plan §3). UI is a thin layer on top.

## Architecture (planned — plan §4–§5)

- `src/core/` — pure mechanics functions: `coverage.ts` (M4), `inheritance.ts` (M1), `spOptimizer.ts` (M2). 100% unit-tested; mechanics tests cite their source (URL/sheet + date).
- `src/sim/` — vendored umalator engine (jalbarrang/umalator-global, **GPL-3.0** — licensing decision in plan §4 applies to the whole repo) + Web Worker glue.
- `src/db/` — Dexie schema, export/import.
- `src/features/` — one folder per module's UI.
- `scripts/` — build-time data pipeline: fetch borrowed JSON from pinned umalator commits → normalize → merge `data-overrides/` → emit `public/data/` (git-tracked, generated).
- `CmPlan` is the cross-module single source of truth (race, targets, deck locks, parents); modules integrate through it (plan §10).

## Working Notes

- Mechanics numbers marked `VALIDATE`/`VERIFY` in plan §5 have conflicting community sources — reconcile against umamily.moe + Ice's sheet before encoding; record outcomes in `docs/mechanics-notes.md`.
- Validation targets: `sparkChance`/inheritance math must reproduce umamily.moe results; skill deltas spot-checked against VFalator (plan §12).
- Target skill lists are variable length (1–7 skills); never assume a fixed count — the priority field drives weighting, not list size.
- No scraping of community sites **by default** (GameTora ToS, ChronoGenesis blocks scrapers — email the operator instead, plan §7). **Private-use exception (owner-authorized, time-boxed, 2026-06-14):** importing from *permissive* feeds is OK **for the maintainer's private use** — uma.guide, Game8 upcoming table, SoulEC/Phoenix published Google-Sheets CSV — to seed data (M4 stat-target defaults, M3 timeline forecast). The **public build swaps these for `ManualStatTargets`/curated JSON before release.** **GameTora + ChronoGenesis stay off-limits / cite-and-deep-link only** regardless. Canonical statement: `docs/superpowers/specs/2026-06-15-shared-data-model.md` §8.
- Each phase must end in a usable artifact (plan §11); Module 4 steps 1–3 are the first milestone.
